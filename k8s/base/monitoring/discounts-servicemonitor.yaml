apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: discounts-servicemonitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: discounts
  endpoints:
    - port: http
      path: /metrics
      interval: 15s