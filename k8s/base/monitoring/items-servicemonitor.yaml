apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: items-servicemonitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: items
  endpoints:
    - port: http
      path: /metrics
      interval: 15s