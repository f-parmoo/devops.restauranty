apiVersion: apps/v1
kind: Deployment
metadata:
  name: items-deployment
spec:
  template:
    spec:
      containers:
        - name: items
          resources:
            requests:
              cpu: "50m"
              memory: "96Mi"
            limits:
              cpu: "200m"
              memory: "192Mi"