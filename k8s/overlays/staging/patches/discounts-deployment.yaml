apiVersion: apps/v1
kind: Deployment
metadata:
  name: discounts-deployment
spec:
  template:
    spec:
      containers:
        - name: discounts
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "150m"
              memory: "128Mi"