apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
spec:
  template:
    spec:
      containers:
        - name: client
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "150m"
              memory: "128Mi"