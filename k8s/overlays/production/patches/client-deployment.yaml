apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deployment
spec:
  template:
    spec:
      containers:
        - name: client
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"