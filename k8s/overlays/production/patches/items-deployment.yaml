apiVersion: apps/v1
kind: Deployment
metadata:
  name: items-deployment
spec:
  template:
    spec:
      containers:
        - name: items
          resources:
            requests:
              cpu: "150m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "384Mi"