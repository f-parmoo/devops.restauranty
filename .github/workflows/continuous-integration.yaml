name: Continuous Integration

on:
  pull_request:
    branches:
      - main

  push:
    branches:
      - main

jobs:
  detect-changes:
    name: Detect Changed Services
    runs-on: ubuntu-latest

    outputs:
      auth: ${{ steps.filter.outputs.auth }}
      items: ${{ steps.filter.outputs.items }}
      discounts: ${{ steps.filter.outputs.discounts }}
      client: ${{ steps.filter.outputs.client }}
      k8s: ${{ steps.filter.outputs.k8s }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Detect changed files
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            auth:
              - 'backend/auth/**'

            items:
              - 'backend/items/**'

            discounts:
              - 'backend/discounts/**'

            client:
              - 'client/**'

            k8s:
              - 'k8s/**'
              - 'infra/**'
              - 'deploy.sh'
              - '.github/workflows/**'

  #############################################
  # AUTH CI
  #############################################

  auth-ci:
    name: Auth CI
    runs-on: ubuntu-latest
    needs: detect-changes

    if: needs.detect-changes.outputs.auth == 'true'

    defaults:
      run:
        working-directory: backend/auth

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format

      - name: Run lint
        run: npm run lint

      - name: Verify docker build
        run: docker build -t auth-ci .

  #############################################
  # ITEMS CI
  #############################################

  items-ci:
    name: Items CI
    runs-on: ubuntu-latest
    needs: detect-changes

    if: needs.detect-changes.outputs.items == 'true'

    defaults:
      run:
        working-directory: backend/items

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format

      - name: Run lint
        run: npm run lint

      - name: Verify docker build
        run: docker build -t items-ci .

  #############################################
  # DISCOUNTS CI
  #############################################

  discounts-ci:
    name: Discounts CI
    runs-on: ubuntu-latest
    needs: detect-changes

    if: needs.detect-changes.outputs.discounts == 'true'

    defaults:
      run:
        working-directory: backend/discounts

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format

      - name: Run lint
        run: npm run lint

      - name: Verify docker build
        run: docker build -t discounts-ci .

  #############################################
  # CLIENT CI
  #############################################

  client-ci:
    name: Client CI
    runs-on: ubuntu-latest
    needs: detect-changes

    if: needs.detect-changes.outputs.client == 'true'

    defaults:
      run:
        working-directory: client

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format

      - name: Run lint
        run: npm run lint

      - name: Verify docker build
        run: docker build -t client-ci .

  #############################################
  # VALIDATE K8S
  #############################################

  validate-k8s:
    name: Validate Kubernetes Manifests
    runs-on: ubuntu-latest
    needs: detect-changes

    if: needs.detect-changes.outputs.k8s == 'true'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup kubectl
        uses: azure/setup-kubectl@v4

      - name: Validate staging overlay
        run: kubectl kustomize k8s/overlays/staging

      - name: Validate production overlay
        run: kubectl kustomize k8s/overlays/production

  #############################################
  # FINAL STATUS
  #############################################

  ci-passed:
    name: CI Passed
    runs-on: ubuntu-latest

    needs:
      - auth-ci
      - items-ci
      - discounts-ci
      - client-ci
      - validate-k8s

    if: always()

    steps:
      - name: Verify CI jobs
        run: |
          if [[ "${{ needs.auth-ci.result }}" == "failure" || \
                "${{ needs.items-ci.result }}" == "failure" || \
                "${{ needs.discounts-ci.result }}" == "failure" || \
                "${{ needs.client-ci.result }}" == "failure" || \
                "${{ needs.validate-k8s.result }}" == "failure" ]]; then
            echo "One or more CI jobs failed"
            exit 1
          fi

          echo "CI passed successfully"